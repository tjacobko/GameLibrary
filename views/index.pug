extends layout

block content
  h1= title
  p Welcome to #[em GameLibrary], a very basic Express website developed as practice for my journey learning ExpressJS.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Games:] !{data.game_count}
      li #[strong Categories:] !{data.category_count}
